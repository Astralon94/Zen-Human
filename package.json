{
  "name": "zen-human-server",
  "version": "0.2.5",
  "private": true,
  "type": "module",
  "description": "Zen-Human (server) — Node http + node:sqlite, zero dipendenze. Sperimentazione migrazione da FSA a DB relazionale.",
  "scripts": {
    "start": "node server.js",
    "build": "vite build && cp dist/index.html public/index.html",
    "dev": "vite",
    "reset-db": "node scripts/reset.mjs",
    "test:roundtrip": "node scripts/roundtrip.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "vite-plugin-singlefile": "^2.0.3"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
