{
  "name": "zen-human",
  "version": "0.2.4",
  "private": true,
  "type": "module",
  "description": "Zen Human — presenze, assenze e prospetto buste paga, offline-first, 100% locale",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4174"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "vite-plugin-singlefile": "^2.0.3"
  }
}
